// Operador Condicional (Ternário)

// Dependendo da condição, nós receberemos valores diferentes

// Condição então valor 1 se não valor 2
// condition? valor1 : valor2

//Exemplos

let pao = true
let queijo = true
const breakfast_ok = pao && queijo ? "Café top" : "Café ruim"
console.log(breakfast_ok)


let age = 16
const drive = age >= 18 ? "Pode dirigir" : "Não pode dirigir"
console.log(drive)