/*
    # O que são parâmetros nas requisições

    São formas de passar para dentro de seu código informações que não estavam lá antes

    as 3 principais formas de se fazer isso é:

    app.route("/").get((req, res) => {
        res.send(req.query.name);
    })
    app.route("/").put((req, res) => {
        res.send(req.body.author);
    })
    app.route("/:parametro").get((req, res) => {
        res.send(req.params.parametro);
    })

    pode-se fazer pelo browser ou pelo body
*/