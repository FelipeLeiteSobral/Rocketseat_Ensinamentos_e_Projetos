/*
    #Consumindo API

      useEffect(() => {
  fetch("https://api.github.com/users/felipeleitesobral")
  .then(response => response.json())
  .then(data=>{
    setUser({
      name: data.name,
      avatars: data.avatar_url,
    })
  })
  .catch(error => console.error(error))
}
*/